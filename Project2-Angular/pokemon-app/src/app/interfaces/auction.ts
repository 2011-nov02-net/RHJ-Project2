export interface Auction
{
    auctionId:string;
    SellerId:string;
    BuyerId:string;
    cardId:string;
    // use | currency in an html element
    PriceSold:number;
    // use | date in an html element
    SellDate:Date;
}
    
