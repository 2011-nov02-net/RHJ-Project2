import { ConvertRarityPipe } from './convert-rarity.pipe';

describe('ConvertRarityPipe', () => {
  it('create an instance', () => {
    const pipe = new ConvertRarityPipe();
    expect(pipe).toBeTruthy();
  });
});
